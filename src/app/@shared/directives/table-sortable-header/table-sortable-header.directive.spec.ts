import { TableSortableHeaderDirective } from './table-sortable-header.directive';

describe('TableSortableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new TableSortableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
